git_url(ENV["MATCH_GIT_URL"] || "")
storage_mode("git")

type("appstore")
app_identifier(["com.innerpearl.pearl"])

# For CI — use App Store Connect API Key
api_key_path(ENV["ASC_API_KEY_PATH"] || "")

readonly(true) # CI should never create new certs
