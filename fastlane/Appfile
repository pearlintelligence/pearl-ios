app_identifier("com.innerpearl.pearl")
apple_id(ENV["APPLE_ID"] || "") # Your Apple Developer email
team_id(ENV["TEAM_ID"] || "")   # Your Apple Developer Team ID

# App Store Connect
itc_team_id(ENV["ITC_TEAM_ID"] || ENV["TEAM_ID"] || "")

# For CI — use App Store Connect API key instead of Apple ID
for_platform :ios do
  for_lane :beta do
    # API key auth is configured in the lane
  end
  for_lane :release do
    # API key auth is configured in the lane
  end
end
